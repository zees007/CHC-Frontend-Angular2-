import { UniqueSerialValidatorDirective } from './unique-serial-validator.directive';

describe('UniqueSerialValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new UniqueSerialValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
